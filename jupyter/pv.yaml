apiVersion: v1
kind: PersistentVolume
metadata:
  name: jupyter-pv-home
  namespace: jupyter
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/jupyter"
